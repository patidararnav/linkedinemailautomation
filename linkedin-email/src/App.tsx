import React from 'react';
import Popup from './components/Popup/Popup'; // Import the popup component
import './App.css'; // Optional: Include styles if needed

const App: React.FC = () => {
    return (
        <div className="App">
            <Popup />
        </div>
    );
};

export default App;
